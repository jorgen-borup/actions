name: 'test'
description: 'Test action'

inputs:
   test:
     description: 'Input test'
     required: false
     default: 10
outputs:
  time:
    description: 'Output value'
runs:
  using: 'node12'
  main: 'app.js'
  
